apiVersion: apps/v1
kind: Deployment
metadata:
  name: shoppingwise-logic-api
  namespace: shoppingwise
  labels:
    app: shoppingwise-logic-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shoppingwise-logic-api
  template:
    metadata:
      labels:
        app: shoppingwise-logic-api
    spec:
      containers:
        - name: logic-api
          image: ghcr.io/knowlesy/shopping-comparison-logic-api:52b0889fb76726475ee06e92dac01748b92542fa
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3001
              name: http
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: shoppingwise-config
                  key: PORT_LOGIC
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: shoppingwise-config
                  key: NODE_ENV
            - name: SCRAPER_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: shoppingwise-config
                  key: SCRAPER_SERVICE_URL
            - name: CLIENT_ORIGIN
              valueFrom:
                configMapKeyRef:
                  name: shoppingwise-config
                  key: CLIENT_ORIGIN
            - name: SCRAPE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: shoppingwise-secret
                  key: SCRAPE_TOKEN
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: shoppingwise-secret
                  key: GEMINI_API_KEY
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: shoppingwise-logic-api
  namespace: shoppingwise
spec:
  selector:
    app: shoppingwise-logic-api
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      name: http
