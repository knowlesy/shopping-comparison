apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: trolleywise-ingress
  namespace: trolleywise
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: trolleywise-client
                port:
                  number: 8080
---
